import { StyleSheet } from "react-native";

const variants = StyleSheet.create({
  title: {
    fontWeight: "bold",
    fontSize: 16,
  },
  subtitle: {
    fontSize: 12,
  },
});

export default variants;
